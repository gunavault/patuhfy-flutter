import 'package:flutter/material.dart';

class CloseRTL extends StatelessWidget {
  const CloseRTL({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: Text('Under Development Close RTL'),
    ));
  }
}
