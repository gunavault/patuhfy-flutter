import 'package:flutter/material.dart';

class ApprovalForm extends StatelessWidget {
  const ApprovalForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: Text('Under Development Approval Form'),
    ));
  }
}
